- name: Install Ruby and Bundler
  hosts: all
  become: true
  roles:
    - app

#  tasks:
#  - name: Install packages
#      #pt:
#        name: "{{ item }}"
#        state: present
#      loop:
#        - ruby-full
#        - ruby-bundler
#        - build-essential
